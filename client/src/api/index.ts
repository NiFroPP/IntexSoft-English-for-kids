import auth from './endpoints/auth.endpoint';
import category from './endpoints/category.endpoint';
import adminPanel from './endpoints/adminPanel.endpoint';

const allEndpoints = {
	auth,
	category,
	adminPanel
};

export default allEndpoints;
